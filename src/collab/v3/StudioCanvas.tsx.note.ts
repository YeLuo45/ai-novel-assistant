/**
 * collab/v3/StudioCanvas.tsx — placeholder note
 * 注：preact 模块在 jsdom test env 不可用，故 .tsx 文件不做 import 烟雾测试。
 * 完整组件逻辑通过 .smoke.test.ts 在 store 层验证。
 * .tsx 文件本身保留，便于在 preact runtime（src/collab/v2）下使用。
 */
export const NOTE = 'preact components — see StudioCanvas.smoke.test.ts for store-level tests'
